Create table country_data (
country VARCHAR(500) PRIMARY KEY,
child_mort float (18,2),
exports float(18,2),
health float(18,2),
imports float(18,2),
income bigint,
inflation float (18,2),
life_expec float(18,2),
total_fert float (18,2),
gdpp bigint,
segment VARCHAR(500)
)